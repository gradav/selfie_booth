# Minimal Flask API Requirements for Selfie Booth
# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1
MarkupSafe==2.1.3

# CORS support for static frontend
Flask-CORS==4.0.0

# Database support
mysql-connector-python==8.2.0

# Messaging services (optional - will gracefully degrade if not available)
twilio==8.9.1

# Image processing
Pillow==10.1.0

# Configuration management
python-dotenv==1.0.0

# For production deployment (optional)
gunicorn==21.2.0

# Note: Removed template-related dependencies since we now use static frontend
# Note: QR code generation moved to frontend JavaScript
# Note: Camera functionality handled by browser APIs
# Minimal Flask API Requirements for Selfie Booth
# Core Flask dependencies - using stable versions
Flask==2.3.3
Werkzeug==2.3.7
MarkupSafe==2.1.3

# CORS support for static frontend
Flask-CORS==4.0.0

# QR Code generation (server-side